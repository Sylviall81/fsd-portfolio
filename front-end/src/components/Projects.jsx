import React from 'react'



export default function Projects() {
  return (
    <div>Projects

{/* <ul className="divide-y divide-gray-200">
      {projects.map((project) => (
        <li key={project.title} className="py-4 flex">
          <img className="h-10 w-10 rounded-full" src={project.image} alt="" />
          <div className="ml-3">
            <p className="text-sm font-medium text-gray-900">{project.description}</p>
            <p className="text-sm text-gray-500">{person.url_repository}</p>
          </div>
        </li>
      ))}
    </ul> */}



    </div>
  )
}
